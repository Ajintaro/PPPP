{
  "name": "pew-pew-pizza-pirates",
  "version": "0.1.0",
  "type": "module",
  "main": "electron/main.js",
  "scripts": {
    "prestart": "node tools/write-version.mjs",
    "start": "env -u NODE_OPTIONS electron .",
    "start:devtools": "env -u NODE_OPTIONS ELECTRON_ENABLE_LOGGING=1 electron ."
  },
  "devDependencies": {
    "electron": "^31.0.0"
  }
}